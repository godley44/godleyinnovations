# godleyinnovations

Create and execute based operations.

## SDK Agent

The repository now includes a basic SDK agent implementation located in `sdk_agent/agent.py`.
You can use it as follows:

```python
from sdk_agent.agent import SDKAgent

agent = SDKAgent(name="Example")
agent.execute("Sample Task")
```
